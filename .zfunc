# ~/.zfunc
# ------------------------------------------------------------------------------

# cat then pipe to less
function catl {
    cat $1 | less
}

# cat then pipe to clipboard
function catc {
    cat $1 | pbcopy
}

# pipe to less
function pless {
    $1 | less
}
